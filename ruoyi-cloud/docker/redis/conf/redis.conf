# Redis 配置 - career-agent
# 数据持久化目录指向数据卷挂载点（属主 uid 999，可写）
dir /data

# RDB 快照文件名
dbfilename dump.rdb

# RDB 快照策略（默认）
save 900 1
save 300 10
save 60 10000

# 快照失败时是否停止写入（目录已修正为可写，保持开启以暴露真实持久化问题）
stop-writes-on-bgsave-error yes
